[project]
name = "x-unblocker"
version = "0.1.0"
description = "A command-line tool to unblock all blocked accounts on X."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "python-dotenv",
    "tweepy",
]
